<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Project</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/daisyui@3.0.0/dist/full.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <%- include('partials/navbar', { page: '' }) %>

  <main class="container mx-auto py-8 px-4">
    <div class="bg-white rounded-lg shadow-md p-6 max-w-3xl mx-auto">
      <h1 class="text-3xl font-bold text-gray-800 mb-2">Edit Project</h1>
      <h2 class="text-xl text-gray-600 mb-6"><%= project.title %></h2>
      
      <form action="/solutions/editProject" method="POST" class="space-y-6">
        <input type="hidden" name="id" value="<%= project.id %>">

        <div class="form-control">
          <label class="label">
            <span class="label-text">Project Title</span>
          </label>
          <input type="text" name="title" value="<%= project.title %>" required class="input input-bordered w-full">
        </div>

        <div class="flex justify-between">
          <a href="/solutions/deleteProject/<%= project.id %>" class="btn btn-error" onclick="return confirm('Are you sure you want to delete this project?')">
            Delete Project
          </a>
          <div class="flex gap-4">
            <a href="/solutions/projects" class="btn btn-ghost">Cancel</a>
            <button type="submit" class="btn btn-primary">Update Project</button>
          </div>
        </div>
      </form>
    </div>
  </main>
</body>
</html>