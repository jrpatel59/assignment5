<!-- Similar structure to addProject.ejs but with values pre-filled -->
<form action="/solutions/editProject" method="POST">
    <input type="hidden" name="id" value="<%= project.id %>">
    <!-- Other fields with values -->
    <input type="text" name="title" value="<%= project.title %>" required>
    <!-- Add Delete button -->
    <a href="/solutions/deleteProject/<%= project.id %>" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded">
        Delete Project
    </a>
</form>